{
  "name": "askai-api",
  "version": "1.0.0",
  "description": "Azure Functions for Ask AI",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@azure/cosmos": "^4.0.0",
    "@azure/functions": "^4.0.0",
    "@azure/openai": "^1.0.0",
    "@azure/ai-content-safety": "^1.0.0",
    "ulid": "^2.3.0"
  },
  "devDependencies": {
    "@azure/functions-core-tools": "^4.0.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}

